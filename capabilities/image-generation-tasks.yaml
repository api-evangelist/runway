naftiko: 1.0.0-alpha2
info:
  label: Runway Image Generation API — Tasks
  description: 'Runway Image Generation API — Tasks. 1 operations. Lead operation: Retrieve task status and output. Self-contained
    Naftiko capability covering one Runway business surface.'
  tags:
  - Runway
  - Tasks
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    RUNWAY_API_KEY: RUNWAY_API_KEY
capability:
  consumes:
  - type: http
    namespace: image-generation-tasks
    baseUri: https://api.dev.runwayml.com/v1
    description: Runway Image Generation API — Tasks business capability. Self-contained, no shared references.
    resources:
    - name: tasks-id
      path: /tasks/{id}
      operations:
      - name: gettask
        method: GET
        description: Retrieve task status and output
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
    authentication:
      type: bearer
      token: '{{env.RUNWAY_API_KEY}}'
  exposes:
  - type: rest
    namespace: image-generation-tasks-rest
    port: 8080
    description: REST adapter for Runway Image Generation API — Tasks. One Spectral-compliant resource per consumed operation,
      prefixed with /v1.
    resources:
    - path: /v1/tasks/{id}
      name: tasks-id
      description: REST surface for tasks-id.
      operations:
      - method: GET
        name: gettask
        description: Retrieve task status and output
        call: image-generation-tasks.gettask
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: image-generation-tasks-mcp
    port: 9090
    transport: http
    description: MCP adapter for Runway Image Generation API — Tasks. One tool per consumed operation, routed inline through
      this capability's consumes block.
    tools:
    - name: retrieve-task-status-and-output
      description: Retrieve task status and output
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: image-generation-tasks.gettask
      outputParameters:
      - type: object
        mapping: $.
